import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    margin: 10,
  },
  textContainer: {
    position: 'absolute',
    alignItems: 'center',
  },
  valueText: {
    fontSize: 22,
    fontWeight: 'bold',
  },
  labelText: {
    fontSize: 14,
    fontWeight: '600',
  },
});
